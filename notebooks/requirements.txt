# Notebook / example-generation tooling only.
#
# Deliberately kept separate from setup.py's install_requires: the
# spark-asammdf connector itself never needs Jupyter, Plotly, or Kaleido at
# runtime, only when regenerating this repository's example notebook and its
# static chart assets under docs/assets/spark-asammdf/.
jupyter
nbclient
nbformat
ipykernel
plotly
kaleido
